---
"note.box": minor
---

Add mobile formatting bar: replaces the bubble toolbar with a fixed bottom bar on touch devices. The bar stays visible while the virtual keyboard is open, with primary actions (headings, bold, italic, link) and a toggle for secondary formatting (underline, strikethrough, code).
